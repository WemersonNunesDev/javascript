/**
 * Operadores Lógicos
 * && -> AND -> E
 * || -> OR -> OU
 * !  -> NOT -> NÃO
 */

// Exemplo:
const numX = 10;
const numY = 2;

const expr = numX - numY;

if(expr >= 12) {
    const maior = 'maior';
    return console.log(maior)
} else if (expr <= 12) {
    const menor = 'menor';
    return console.log(menor)
}
