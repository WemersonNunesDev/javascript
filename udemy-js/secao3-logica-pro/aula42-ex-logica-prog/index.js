// x = largura
// y = altura
//  criar uma função "ePaisagem" que recebe dois argumentos (x, y).
// Retorna true se a paissagem estiver no modo retrato.

const ePaisagem = (lar, alt) => lar > alt;
// return lar > alt ? true : false;

console.log(ePaisagem(1920, 1080));
